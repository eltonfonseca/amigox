
<% if flash[:alert] %>
    <div class="notice"><%= flash[:alert] %></div>
  <% end %>

<div id="page-content-wrapper">
    <div class="container-fluid">
      <h1 class="mt-2">Usuários</h1>
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th colspan="3"></th>
          </tr>
        </thead>
  
        <tbody>
          <% @users.each do |user| %>
          <tr>
            <td><%= user.name %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
      <% if current_user.id == session[:group_id]%>
        <%= link_to 'Sortear', group_raffle_path(session[:group_id]), method: :post, class: 'btn btn-success' %>
      <% end %>
    </div>
  </div>